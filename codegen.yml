overwrite: true
schema: ./schemas/schema.graphql
documents:
  - ./routes/**/*.tsx
  - ./islands/**/*.tsx
generates:
  schemas/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-resolvers
      - typescript-operations
