overwrite: true
schema: ./schemas/schema.graphql
documents:
  - ./routes/**/*.tsx
generates:
  schemas/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-resolvers
      - typescript-operations
